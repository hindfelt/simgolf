import {test,expect} from '@playwright/test';
import {originalActorMotionContext} from '../src/simulation/original-actor-motion-context.js';
import {originalActorBallMotion} from '../src/simulation/original-actor-ball-motion.js';
function fixture(){const actors=Array.from({length:152},()=>new Uint8Array(256)),a=new DataView(actors[0].buffer);a.setInt32(0xdc,20992,true);a.setInt32(0xe0,20992,true);a.setInt32(0xe4,300,true);a.setInt32(0xec,160,true);const terrain=new Uint8Array(2500).fill(2),tileFlags=new Uint16Array(2500),edgeMasks=new Uint8Array(2500);tileFlags[1020]=123;edgeMasks[1020]=5;return {actors,actorId:0,terrain,tileFlags,edgeMasks,metadata:[null,null,{bounceCoefficient:4,rollCoefficient:3,scatterCoefficient:0}],visualSlot:-1,phaseCounter:0,variant:0,luck:0,difficulty:0,seed:1};}
test('captures current tile metadata and resets the per-step centre flag',()=>{const s=fixture();s.ballTile={x:1,z:1};s.ballTerrain=99;s.centreFlag=1;const r=originalActorMotionContext(s);expect(r.ballTile).toEqual({x:20,z:20});expect(r.ballTerrain).toBe(2);expect(r.terrainFlags).toBe(123);expect(r.edgeFlags).toBe(5);expect(r.rollCoefficient).toBe(3);expect(r.centreFlag).toBe(0);expect(s.centreFlag).toBe(1);});
test('context feeds actual composed actor motion without manually copying coefficients',()=>{const r=originalActorBallMotion(originalActorMotionContext(fixture()),(_,state)=>({state,value:0}));expect(r.next).toBe('0x4295ef');const a=new DataView(r.state.actors[0].buffer);expect(a.getInt32(0xe0,true)).toBe(20982);expect(a.getInt32(0xf0,true)).toBe(-64);expect(a.getInt32(0xec,true)).toBe(155);});
test('rejects missing metadata rather than substituting a default surface',()=>{const s=fixture();s.metadata=[];expect(()=>originalActorMotionContext(s)).toThrow('coefficients');});
